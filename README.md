# Escalated — Laravel Support Ticket System

An embeddable, open-source support ticket system for Laravel applications. Supports self-hosted, synced, and cloud hosting modes.

## Installation

```bash
composer require escalated/escalated-laravel
php artisan escalated:install
php artisan migrate
```